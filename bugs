Bugs Fixed =>
1. just managers can create ssh keys ( 3 hours )
2. make logs readable ( 3 hours )
3. adding predefined keys to database ( 4 hours )
4. create log of uploading files ( 1 hour )
5. download from server ( 5 hours )
6. when removing a manager's account, all ssh keys would remove. ( 1 hour )  
7. opening more than 8 terminals has problems ( 5 hours )
8. overwrite current line (not going to nextline) ( 4 hours )
9. when pressing 'ctrl + l' to clean terminal, cursur goes to url bar. ( 2 hours )
10. some shortcuts like 'alt + backspace' not works normally. ( 1 hour )
11. when scrolling in VIM , marginal texts (percentages) come up with screen. ( 4 hours )
12. buffer of scroll in terminals is not enough ( 1 hour )
13. log of uploaded file ( renaming the file, saving the file in "target/upload" , printing source and destination in log ) ( 2 hours )
14. capture keyboard of client, so log of vim and nano will be readable. ( 5 hours )
15. check existance of "authorized_keys" and "ssh_config" and "sshd_config" in commands. ( 1 hour )


Primary Bugs => 
	a:
		1. in some special cases, connects to wrong server. (when using connect to host.)
		2. paste text using mouse middle btn.

	b:
		3. when pasting in Nano, goes to next lines. 
		4. can't open more than one tab of bastillion. 
		5. account login problem ( can't login to account after sometime )


( RADIUS )
( database )


Security Bugs =>
1. log from downloaded files ( delete upload & download stored files after a time (like download) )


Ignored Bugs =>
